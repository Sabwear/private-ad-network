-- Project-specific development records should be added after creating local Auth users
-- through Supabase Studio or the Auth Admin API. The baseline policy is part of the
-- migration so remote and local environments always share the same rules.
select 1;
